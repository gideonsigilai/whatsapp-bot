name: Deploy to Cloudflare Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    name: Deploy Dashboard → Cloudflare Pages
    runs-on: ubuntu-latest

    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate secrets
        run: |
          echo "🔐 Checking required secrets..."
          [ -n "${{ secrets.CLOUDFLARE_API_TOKEN }}"  ] && echo "  ✅ CLOUDFLARE_API_TOKEN is set"  || (echo "  ❌ CLOUDFLARE_API_TOKEN is MISSING" && exit 1)
          [ -n "${{ secrets.CLOUDFLARE_ACCOUNT_ID }}" ] && echo "  ✅ CLOUDFLARE_ACCOUNT_ID is set" || (echo "  ❌ CLOUDFLARE_ACCOUNT_ID is MISSING" && exit 1)
          echo "  ℹ️  Pages project: wa-bot-dashboard"

      - name: Verify dashboard assets
        run: |
          echo "📁 Verifying public/ assets... "
          test -f public/index.html && echo "  ✅ index.html"
          test -f public/docs.html  && echo "  ✅ docs.html"
          test -f public/style.css  && echo "  ✅ style.css"
          test -f public/app.js     && echo "  ✅ app.js"

      - name: Install Wrangler
        run: npm install -g wrangler

      - name: Ensure Pages project exists
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          echo "🏗️  Creating project if it doesn't exist..."
          wrangler pages project create wa-bot-dashboard \
            --production-branch=main \
            || echo "  ℹ️ Project already exists, continuing..."

      - name: Deploy to Cloudflare Pages
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          wrangler pages deploy public \
            --project-name=wa-bot-dashboard \
            --commit-dirty=true

      - name: Deployment summary
        run: |
          echo ""
          echo "✅ Dashboard deployed to Cloudflare Pages!"
          echo "   Project: wa-bot-dashboard"
          echo "   Branch:  ${{ github.ref_name }}"
          echo "   Commit:  ${{ github.sha }}"
